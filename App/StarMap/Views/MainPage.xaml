<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="StarMap.Views.MainPage">

  <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
    <OpenGLView BackgroundColor="Aqua" HeightRequest="100" WidthRequest="100">
      <OpenGLView.GestureRecognizers>
        <TapGestureRecognizer
          Command="{Binding SelectStarCommand}" />
      </OpenGLView.GestureRecognizers>
    </OpenGLView>

    <Button Text="Show details" Command="{Binding ShowStarDetailsCommand}" />
    <Button Text="Move to star" />
  </StackLayout>


</ContentPage>