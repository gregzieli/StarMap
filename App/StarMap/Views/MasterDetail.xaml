<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
                  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                  xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
                  prism:ViewModelLocator.AutowireViewModel="True"
                  x:Class="StarMap.Views.MasterDetail">

  <MasterDetailPage.Master>
    <ContentPage Title="Default" Opacity="0.8">
      <StackLayout Padding="20">
        <Button Text="Stars" Command="{Binding NavigateCommand}" CommandParameter="NavigationPage/MainPage" />
        <Button Text="Settings" Command="{Binding NavigateCommand}" CommandParameter="NavigationPage/SettingsPage" />


        <ListView ItemsSource="{Binding Constellations}" HeightRequest="200">
          <ListView.HeaderTemplate>
            <DataTemplate>
              <StackLayout Orientation="Horizontal">
                <Button Text="Constellations" />
              </StackLayout>              
            </DataTemplate>
          </ListView.HeaderTemplate>
          <ListView.ItemTemplate>
            <DataTemplate>
              <TextCell Text="{Binding Name}" />
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>
        
        
        
      </StackLayout>
    </ContentPage>
  </MasterDetailPage.Master>

</MasterDetailPage>