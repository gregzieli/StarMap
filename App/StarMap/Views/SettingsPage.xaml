<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="StarMap.Views.SettingsPage"
             Title="Settings">

  <StackLayout Padding="10">
    <!-- TODO: create a custom template for sections -->
    <Label Text="Coordinates" FontAttributes="Bold" FontSize="Large"/>
    <StackLayout Orientation="Horizontal" HeightRequest="20">
      <Button Command="{Binding UpdateLocationCommand}" Image="refresh.png" WidthRequest="24" />
      <!-- Although binding uses ToString, this StringFormat part here is needed. -->
      <Label Text="{Binding GeoPosition.DmsLatitude, StringFormat='{0}'}" />
      <Label Text="{Binding GeoPosition.DmsLongitude, StringFormat='{0}'}" />
      <ActivityIndicator IsRunning="{Binding IsBusy}" />
    </StackLayout>
  </StackLayout>
  
</ContentPage>